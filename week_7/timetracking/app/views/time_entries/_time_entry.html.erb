[<%= link_to entry.id, edit_project_time_entry_path(project, entry) %>]
<%= entry.hours %> h
<%= entry.minutes %> m -
<%= formatted_date(entry.date) %>
:: <%= entry.comments %>
:: <%= link_to "Show", project_time_entry_path(project, entry) %>
